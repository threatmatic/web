---
import Layout from '../layouts/Layout.astro';
import Header from '../components/header.astro';
import '../src/styles/global.css'

import tm_circle from '../assets/tm_circle.png';
import { Fingerprint } from 'lucide-astro';
import { Clock } from 'lucide-astro';
---

<Layout>
  <!-- <Header /> -->

  <div class="container">
    <!-- <div class="max-h-screen max-w-max text-center relative m-10">
      <div class="absolute flex flex-row justify-between items-center m-7 inset-0 w-96 h-96 bg-pink-500 text-center">
        <div id="scaleElement" class="transform transition-transform duration-700 ease-in-out">
          <div class="h-24 w-24 bg-white p-2 mx-6">amber-100</div>
        </div>
      </div>
    </div> -->

    <div class="m-10"> 
      <button class="bg-red-100 p-5 hover:bg-red-500 transition  ease-in-out">test</button>


    </div>
    <div class="m-10">
      <button class="bg-blue-400 p-5 rounded-lg  hover:bg-blue-950 hover:text-white transition-all duration-1000 ease-in-out">Button A</button>
    </div>
   
    <div class="m-10">
      <button class="bg-blue-400 p-5 rounded-lg  hover:bg-blue-950 hover:text-white hover:rotate-180 transition-transform duration-1000">Button A</button>
    </div>

    <div class="m-10">
      <button class="bg-blue-400 p-5 rounded-lg  hover:bg-blue-950 hover:text-white hover:scale-110 transition-transform duration-1000">Button A</button>
    </div>


    <div class="m-10">
      <button id ="scaleElement" class="bg-blue-400 p-5 rounded-lg  hover:bg-blue-950 hover:text-white hover:scale-110 transition-transform -translate-x-full duration-1000">Button A</button>
    </div>
        
    </div>
  
  </div>

  <script>
    // Wait for the DOM to load
    window.addEventListener('load', function() {
      const scaleElement = document.getElementById('scaleElement');
      
      // Ensure the element exists
      if (scaleElement) {
        // Apply translate class and trigger transition
        scaleElement.classList.add('translate-x-0'); // Apply translation on load
      }
    });
  </script>
</Layout>
